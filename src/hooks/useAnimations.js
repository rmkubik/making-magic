import React, { useState } from "react";

// reqs:
// tells the rest of the game once all animations are done
// an animation is just any old promise that does whatever it needs to do
// - once the promise resolves, it removes the animation from the list
// we can add as many animations as we want in a render pass
//

const useAnimations = () => {
  const animations = [];
};

export default useAnimations;
